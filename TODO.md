# 待完成任务
- 添加一个表用于存储文件名名为曾下载文件名库，用于存储一些文件的文件名，前端添加一个批量录入曾下载文件名的页面， 提供一个巨大输入框和录入按钮，录入时以换行符为间隔隔开每一个文件名
- 添加一个批量文件名查重页面，提供一个巨大输入框用于填写需要查重的文件名数组（以换行为间隔），提供一个查重按钮， 点击后查找历史索引库和曾下载文件名库中匹配的文件名，如果有结果，则跳转到查重展示页面，以查重文件名对匹配文件名 一对多的关系展示茶从结果，列名分别为查重文件名，匹配文件名（一个一行），匹配路径（一个一行）
其中查重文件名提供超链接，跳转到设置中的拼接前缀拼接网址+文件名